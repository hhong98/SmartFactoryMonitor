<UserControl
    x:Class="SmartFactoryMonitor.Controls.EquipListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SmartFactoryMonitor.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="210" />
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="80" />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Padding" Value="0,10" />
            </Style>
        </Grid.Resources>

        <Border
            x:Name="EquipStatus"
            Grid.Column="0"
            Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" />
        <TextBlock
            Grid.Column="1"
            Margin="10,0,0,0"
            HorizontalAlignment="Left"
            Text="{Binding EquipName}" />
        <TextBlock
            Grid.Column="2"
            HorizontalAlignment="Left"
            Text="{Binding IpAddress}" />
        <TextBlock
            Grid.Column="3"
            HorizontalAlignment="Center"
            Text="{Binding Port}" />
        <TextBlock
            Grid.Column="4"
            HorizontalAlignment="Center"
            Text="{Binding IsActive}" />
        <CheckBox
            Grid.Column="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsChecked="{Binding IsChecked}"
            Visibility="{Binding IsPanelOpened, Source={StaticResource MainVM}, Converter={StaticResource InvBoolToVis}}" />
    </Grid>
</UserControl>