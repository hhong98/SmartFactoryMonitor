<Window x:Class="SmartFactoryMonitor.MainWindow"
        x:Name="MainWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartFactoryMonitor"
        xmlns:tool="http://schemas.xceed.com/wpf/xaml/toolkit"
        DataContext="{StaticResource MainVM}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5" Width="900" Height="700">

    <Grid x:Name="Root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--#region Sidebar-->
        <Grid Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderBrush="{StaticResource SidebarHoverBrush}" BorderThickness="0,0,0,1">
                <TextBlock
                Text="🏭 SMART FACOTRY MONITOR"
                TextAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{StaticResource TextOnPrimaryBrush}" />
            </Border>
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="150" />
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource SidebarBackgroundBrush}" />
                        <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}" />
                        <Setter Property="Padding" Value="15,10" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          RecognizesAccessKey="True" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarHoverBrush}" />
                                            <Setter Property="FontSize" Value="14" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="{StaticResource PrimaryBlueDarkBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>
                <!--#region Navigation-->
                <StackPanel Grid.Row="0" Orientation="Vertical" VerticalAlignment="Top">
                    <Button x:Name="BtnMonitor" Content="🖥  설비 모니터링" Click="BtnMonitor_Click" />
                    <Button x:Name="BtnShowList" Content="🔧  설비 관리" Click="BtnShowList_Click" />
                </StackPanel>
                <!--#endregion Navigation-->
                <!--#region System-->
                <Border Grid.Row="1" BorderBrush="{StaticResource SidebarHoverBrush}" BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Vertical"  VerticalAlignment="Top" Margin="0,10,0,0">
                        <Button Content="⚙️  설정" />
                    </StackPanel>
                </Border>
                <!--#endregion System-->
            </Grid>
        </Grid>
        <!--#endregion Sidebar-->

        <!--#region Content-->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#007ACC">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}" />
                        </Style>
                    </Grid.Resources>
                    
                    <!--#region Content Header-->
                    <TextBlock Text="SMART FACTORY MONITOR"
                               FontSize="18"
                               Grid.Column="0"
                               Foreground="White"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                        <TextBlock Text="●" Foreground="Yellow" Margin="0,0,5,0" />
                        <TextBlock Text="Online" />
                    </StackPanel>
                    <!--#endregion Content Header-->
                </Grid>
            </Border>
            
            <!--Content Body-->
            <Frame Grid.Row="1" x:Name="MainFrame" NavigationUIVisibility="Hidden" />
        </Grid>
        <!--#endregion Content-->
    </Grid>
</Window>